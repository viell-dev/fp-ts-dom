/**
 * It's not currently possible to represent this type since it should contain
 * `VideoFrame` since it's currently out-of-scope for this project and missing
 * in the TypeScript types.
 */
export type THtmlCanvasImageSource = unknown;
