/**
 * It's currently not possible to represent this type since it should contain
 * `WebGLRenderingContext`, `WebGL2RenderingContext` and `GPUCanvasContext`
 * since they are currently out-of-scope for this project. Or in
 * `GPUCanvasContext`'s case; missing in the TypeScript types.
 */
export type THtmlRenderingContext = unknown;
