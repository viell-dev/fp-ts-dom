/**
 * It's currently not possible to represent this type since it should contain
 * `OffscreenCanvasRenderingContext2D` and `GPUCanvasContext` since they are
 * missing in the TypeScript types.
 */
export type MissingOffscreenRenderingContext = unknown;

/**
 * It's currently not possible to represent this type since it should contain
 * `OffscreenCanvasRenderingContext2D` and `GPUCanvasContext` since they are
 * currently out-of-scope for this project and missing in the TypeScript types.
 */
export type THtmlOffscreenRenderingContext = unknown;
