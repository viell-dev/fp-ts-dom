/**
 * "Media Capture and Streams" and "Media Source Extensions™" specs are
 * currently out-of-scope and the "File API" spec will always be out-of-scope.
 */
export type THtmlMediaProvider = MediaStream | MediaSource | Blob;
